import { styled } from 'styled-components';

export const Title = styled.h2`
  font-size: 24px;
  font-weight: 600;
  line-height: normal;
  color: ${p => p.theme.color.text};
  margin-bottom: 24px;
`;
